services:
  copilot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      - DB_PATH=/data/copilot.db
    volumes:
      - copilot-data:/data
      - ./lessons:/lessons
    restart: unless-stopped

volumes:
  copilot-data:
